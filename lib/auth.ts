// Stub — routes that import this module are pending a real auth integration.
import { NextRequest } from "next/server";
export async function getAuthUser(_req: NextRequest): Promise<null> {
  return null;
}
